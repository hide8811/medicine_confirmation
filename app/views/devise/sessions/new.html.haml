.login-user
  = link_to "新規登録", new_registration_path(resource_name), class: 'login-user-new'

  .login-user-title
    ログイン

  .login-user-form
    = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
      .login-user-employee
        = f.label :employee_id, '社員ID', class: 'login-user-employee__title-label'
        = f.text_field :employee_id, autofocus: true, autocomplete: "username", class: 'login-user-employee__input-text'

      .login-user-password
        = f.label :password, 'パスワード', class: 'login-user-password__title-label'
        = f.password_field :password, autocomplete: "current-password", class: 'login-user-password__input-password'

      .login-user-actions
        = f.submit "ログイン", class: 'user-login-button'
